git config --global user.name "Dhruvvv"

git config --global user.name


git config --global user.emal "dhruvpanchal547@gmail.com"

git config --global user.emal

cd

cd

cd

mkdir git-demo

cd git-demo

git init

echo "HelloWorld">file.txt

git add .

git commit -m "Initial Commit"

git status

git log




mkdir fork_test && cd fork_test

git clone https://github.com/Dhruvvv-p/copied.git


cd copied

git checkout -b feature

echo "Feature Added">newtext.txt

git add .

git commit -m "Added new Files"

git checkout main

git merge feature


git push origin feature

git push origin main







